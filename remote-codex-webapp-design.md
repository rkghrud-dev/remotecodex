# 원격 Codex 지원 웹앱 설계 초안

## 1. 목표

컴퓨터를 잘 다루지 못하는 지인의 PC에 작은 프로그램을 설치해 두고, 내 웹앱에서 그 PC에 접속하여 PowerShell, Codex CLI, 파일 전송, 필요 시 음성 통화를 통해 원격으로 개발 지원을 할 수 있게 만든다.

핵심 목표는 일반 원격 제어 프로그램보다 설치와 사용을 단순하게 만드는 것이다.

## 2. 기본 구조

```text
내 컴퓨터 브라우저
  |
  | HTTPS / WebSocket
  v
웹앱 서버
  |
  | WebSocket
  v
상대방 PC 에이전트
  |
  +-- PowerShell 실행
  +-- Codex CLI 실행
  +-- 파일 읽기/쓰기/전송
  +-- 마이크 음성 전송
```

웹브라우저만으로는 상대 컴퓨터의 PowerShell 실행이나 로컬 파일 수정 권한을 안정적으로 줄 수 없으므로, 상대 PC에는 Windows용 에이전트 프로그램을 설치한다.

## 3. 구성 요소

### 3.1 웹앱

내가 접속해서 사용하는 관리자 화면이다.

필요 기능:

- 상대 PC 접속 상태 확인
- 접속된 PC 목록 보기
- PowerShell 터미널 열기
- 명령 실행 결과 실시간 확인
- Codex CLI 실행
- 파일 업로드
- 파일 다운로드
- 상대 PC 폴더 탐색
- 간단한 로그 확인
- 음성 통화 시작/종료

### 3.2 웹앱 서버

웹 화면과 상대 PC 에이전트 사이의 중계 서버다.

필요 기능:

- 로그인
- 상대 PC 등록
- 에이전트 연결 관리
- WebSocket 세션 관리
- 명령 전달
- 터미널 출력 전달
- 파일 전송 중계
- 실행 로그 저장
- 연결 토큰 관리

### 3.3 상대 PC 에이전트

상대방 컴퓨터에 설치되는 프로그램이다.

필요 기능:

- 서버에 자동 접속
- PC 이름, 사용자명, OS 정보 전송
- PowerShell 명령 실행
- 실행 결과 실시간 전송
- Codex CLI 실행
- 파일 업로드 수신
- 파일 다운로드 요청 처리
- 폴더 목록 조회
- 부팅 시 자동 실행
- 에이전트 종료 기능

## 4. MVP 범위

처음부터 모든 기능을 넣지 않고, 1차 버전은 아래 기능만 만든다.

### 1차 MVP

- 상대 PC 에이전트 설치
- 에이전트가 서버에 접속
- 웹앱에서 접속 상태 확인
- 웹 터미널에서 PowerShell 명령 실행
- 명령 출력 실시간 표시
- 파일 업로드
- 파일 다운로드
- 간단한 실행 로그 저장

### 2차 기능

- Codex CLI 실행 전용 화면
- 프로젝트 폴더 선택
- 파일 탐색기 UI
- 웹에서 파일 내용 보기/수정
- 명령 템플릿
- 에이전트 자동 업데이트

### 3차 기능

- 음성 통화
- 화면 공유
- 원격 클릭/키보드 입력
- 여러 PC 동시 관리
- 권한별 사용자 관리

## 5. 권장 기술 스택

### 웹앱

- Frontend: Next.js 또는 React
- Backend: Node.js, FastAPI, Go, C# ASP.NET 중 선택
- Realtime: WebSocket 또는 SignalR
- DB: SQLite 또는 PostgreSQL
- 파일 저장: 서버 로컬 디스크 또는 S3 호환 스토리지

### Windows 에이전트

권장 후보:

- C#
  - Windows 제어와 PowerShell 실행에 편함
  - SignalR과 궁합이 좋음
- Go
  - 단일 exe 배포가 쉬움
  - 에이전트를 가볍게 만들기 좋음

초기 개발 추천 조합:

```text
웹앱: Next.js
서버: C# ASP.NET + SignalR
에이전트: C# Windows 앱 또는 콘솔 앱
DB: SQLite
```

개인용으로 빠르게 만들기에는 C# 기반이 가장 단순하다.

## 6. 주요 화면 설계

### 6.1 로그인 화면

- 관리자 로그인
- 초대 코드 생성 기능은 관리자 전용

### 6.2 PC 목록 화면

표시 항목:

- PC 이름
- 사용자명
- 접속 상태
- 마지막 접속 시간
- OS 정보
- 에이전트 버전

주요 버튼:

- 터미널 열기
- 파일 보기
- Codex 실행
- 음성 연결
- 로그 보기

### 6.3 터미널 화면

기능:

- PowerShell 명령 입력
- 실시간 출력 표시
- 현재 작업 폴더 표시
- 명령 히스토리
- 중단 버튼

### 6.4 Codex 실행 화면

기능:

- 프로젝트 폴더 선택
- Codex 프롬프트 입력
- 실행 결과 확인
- 생성/수정된 파일 목록 확인

예시 명령:

```powershell
cd C:\Users\상대방\Desktop\project
codex "이 프로젝트 오류를 확인하고 수정해줘"
```

### 6.5 파일 전송 화면

기능:

- 상대 PC 폴더 탐색
- 파일 업로드
- 파일 다운로드
- 새 폴더 만들기
- 파일 삭제
- 텍스트 파일 간단 편집

## 7. 에이전트 동작 방식

에이전트는 실행되면 서버에 연결한다.

```text
1. 에이전트 실행
2. 저장된 토큰으로 서버에 접속
3. 서버에 PC 정보 등록
4. 웹앱에서 명령을 받음
5. PowerShell 프로세스 실행
6. stdout/stderr를 서버로 실시간 전송
7. 명령 종료 상태를 서버에 보고
```

PowerShell 실행 시 기본 작업 폴더는 사용자 홈 또는 지정된 프로젝트 폴더로 둔다.

## 8. 설치 흐름

상대방이 컴퓨터를 잘 못한다는 전제를 기준으로 설치 과정을 최대한 단순하게 만든다.

```text
1. 내가 웹앱에서 초대 코드 생성
2. 상대방에게 설치 파일 링크 전달
3. 상대방이 설치 파일 실행
4. 초대 코드 입력
5. 설치 완료
6. 이후 에이전트는 자동 실행
```

가능하면 설치 파일은 한 개의 `.exe` 또는 `.msi`로 만든다.

## 9. 최소 보안 설계

개인용이라도 아래 정도는 반드시 넣는다.

- 관리자 로그인
- PC별 고유 토큰
- 초대 코드는 1회용
- 초대 코드 만료 시간 설정
- 모든 명령 실행 로그 저장
- 에이전트에서 연결 해제 버튼 제공
- 서버와 에이전트 통신은 HTTPS/WSS 사용

보안을 복잡하게 만들 필요는 없지만, 실수로 다른 사람이 접속할 수 있는 구조는 피한다.

## 10. 파일 전송 설계

업로드:

```text
웹앱 -> 서버 -> 상대 PC 에이전트 -> 지정 폴더 저장
```

다운로드:

```text
상대 PC 에이전트 -> 서버 -> 웹앱 브라우저
```

큰 파일은 조각 단위로 나누어 전송한다.

## 11. 음성 기능 설계

음성 통화는 WebRTC를 사용하는 것이 적합하다.

초기에는 아래 중 하나로 처리한다.

옵션 A:

- 웹앱과 상대 PC 에이전트 사이에 WebRTC 연결
- 에이전트가 마이크 입력을 캡처
- 내 브라우저로 음성 스트리밍

옵션 B:

- 음성 기능은 별도 링크를 사용
- 예: 웹앱 안에서 임시 음성방 생성
- 구현 난이도가 낮아질 수 있음

MVP에서는 음성 기능을 제외하고, 터미널과 파일 전송을 먼저 완성한다.

## 12. 개발 순서

1. 서버 기본 프로젝트 생성
2. 관리자 로그인 구현
3. 에이전트 등록 API 구현
4. WebSocket 또는 SignalR 연결 구현
5. Windows 에이전트에서 서버 연결
6. 웹에서 명령 보내기
7. 에이전트에서 PowerShell 실행
8. 출력 실시간 표시
9. 파일 업로드/다운로드 구현
10. Codex CLI 실행 편의 기능 추가
11. 설치 파일 제작
12. 음성 기능 검토

## 13. 우선 결정할 것

- 서버를 어디에 둘지
  - 내 PC
  - VPS
  - 클라우드 서버
- 에이전트 언어
  - C#
  - Go
- 웹앱 프레임워크
  - Next.js
  - ASP.NET MVC/Blazor
- 설치 방식
  - 단일 exe
  - MSI 설치 프로그램
- 음성 기능을 MVP에 포함할지 여부

## 14. 현재 추천 방향

처음 버전은 아래 방향으로 진행한다.

```text
서버/백엔드: ASP.NET Core + SignalR
웹 UI: React 또는 Blazor
Windows 에이전트: C#
DB: SQLite
통신: HTTPS + SignalR WebSocket
MVP 기능: PowerShell 원격 실행 + 파일 전송
```

이 방향은 Windows 제어, PowerShell 실행, 실시간 통신을 한 기술권 안에서 처리할 수 있어 초기 구현 난이도가 낮다.

## 15. 임시 프로젝트 이름 후보

- Remote Codex Helper
- Codex Remote Assist
- Friend Dev Assist
- Easy Remote Dev
- PowerCodex Remote
